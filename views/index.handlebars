<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-4">The Ringer Scraper</h1>
        <a href="/scrape">
            <button class="btn btn-primary">Scrape</button>
        </a>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col-lg-9 col-sm-12">
            <h3>Article Results</h3>
            <div class="articleList">

                {{#each articles}} {{> list/listBlock }} {{/each}}
            </div>
        </div>

    </div>
    {{!--
    <div class="col-lg-2">
        <div id="comments">

        </div>
    </div> --}}
</div>

{{!--
<div class="container-fluid">
    <div class="row">
        <div class="col-lg-10 col-sm-12">
            <h3>Comment</h3>
            <div class="commentList">

                {{#each comments}} {{> list/comment }} {{/each}}
            </div>
        </div>

    </div>
</div> --}}

<!-- Modal -->
<div class="modal fade" id="commentModal" tabindex="-1" role="dialog" aria-labelledby="commentsModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content modal-launch">
            <div class="modal-header">
                <div class="modal-title"></div>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                
                {{!-- <div class="modalButton"></div> --}}




            </div>
            {{!-- <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div> --}}
        </div>
    </div>
</div>